from setuptools import setup, find_packages


setup(
    name="JSONDataClasses",
    description="Module for simple json data description with python classes",
    python_requires=">=3.11",
    packages=find_packages()
)
